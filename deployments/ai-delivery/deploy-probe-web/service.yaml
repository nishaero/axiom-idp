apiVersion: v1
kind: Service
metadata:
  name: probe-web
  namespace: axiom-apps
  labels:
    app.kubernetes.io/name: probe-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: probe-web
  ports:
    - name: http
      port: 80
      targetPort: 80
