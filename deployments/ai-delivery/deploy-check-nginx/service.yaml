apiVersion: v1
kind: Service
metadata:
  name: check-nginx
  namespace: axiom-apps
  labels:
    app.kubernetes.io/name: check-nginx
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: check-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
