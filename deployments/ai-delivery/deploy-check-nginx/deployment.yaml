apiVersion: apps/v1
kind: Deployment
metadata:
  name: check-nginx
  namespace: axiom-apps
  labels:
    app.kubernetes.io/name: check-nginx
    app.kubernetes.io/managed-by: axiom-idp
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: check-nginx
  template:
    metadata:
      labels:
        app.kubernetes.io/name: check-nginx
        app.kubernetes.io/managed-by: axiom-idp
    spec:
      automountServiceAccountToken: false
      containers:
        - name: check-nginx
          image: nginx:1.27-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          securityContext:
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
