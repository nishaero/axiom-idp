apiVersion: v1
kind: Service
metadata:
  name: stable-job-check
  namespace: axiom-apps
  labels:
    app.kubernetes.io/name: stable-job-check
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: stable-job-check
  ports:
    - name: http
      port: 80
      targetPort: 80
