apiVersion: v1
kind: Service
metadata:
  name: demo-web-final
  namespace: axiom-apps
  labels:
    app.kubernetes.io/name: demo-web-final
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo-web-final
  ports:
    - name: http
      port: 80
      targetPort: 80
