apiVersion: v1
kind: Service
metadata:
  name: ui-smoke
  namespace: axiom-apps
  labels:
    app.kubernetes.io/name: ui-smoke
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ui-smoke
  ports:
    - name: http
      port: 80
      targetPort: 80
