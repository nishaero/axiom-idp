apiVersion: v1
kind: Service
metadata:
  name: demo-web
  namespace: axiom-apps
  labels:
    app.kubernetes.io/name: demo-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo-web
  ports:
    - name: http
      port: 80
      targetPort: 80
