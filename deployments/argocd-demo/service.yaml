apiVersion: v1
kind: Service
metadata:
  name: argocd-demo-web
  namespace: argocd-demo
  labels:
    app.kubernetes.io/name: argocd-demo-web
spec:
  selector:
    app.kubernetes.io/name: argocd-demo-web
  ports:
    - name: http
      port: 80
      targetPort: 80
